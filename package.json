{
  "name": "soundify-api-server",
  "version": "1.0.0",
  "description": "API server for Soundify app",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch database/db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}